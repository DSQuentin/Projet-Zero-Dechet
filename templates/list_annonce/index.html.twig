{% extends 'base.html.twig' %}

{% block title %}Liste des annonces{% endblock %}

{% block body %}

    <div>
        <h1 class="text-2xl font-bold mx-10 my-4">Liste des annonces</h1>
        <div class="grid gap-x maw-w-lg mx-auto grid-cols-3 maw-w-none">
            {% for annonce in pagination %}
                {% include 'list_annonce/_card.html.twig' %}
            {% endfor %}
        </div>
        <div class="flex justify-center">
            {{ knp_pagination_render(pagination, '_pagination.html.twig') }}
        </div>
    </div>



{% endblock %}
