<div class="">
<h1 class="px-4 pt-3 pb-2 text-blue-500 font-bold text-lg">Ajouter un nouveau commentaire</h1>
{% form_theme commentForm 'themes/_form.html.twig' %}
{{ form_start(commentForm) }}
    {{ form_widget(commentForm) }}
    <div class="flex justify-end mb-4">
        <button class="bg-blue-400 px-3 py-1 rounded-lg text-white hover:bg-gray-200 hover:text-blue-400">{{ button_label|default('Commenter') }}</button>
    </div>
{{ form_end(commentForm) }}
</div>
